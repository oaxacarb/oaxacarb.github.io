<%= Rouge::Themes::ThankfulEyes.render(:scope => '.highlight') %>

pre.highlight {
  margin-bottom: 1em;

  code {
    display: block;
    padding: 1em !important;
    background-color: inherit;
    border: inherit;
    color: inherit;
    padding: inherit;
  }
}

.example {
  pre.highlight {
    padding: 3em;
  }
}
